## Numeric Behavior

* **Menu Selection**: Accepts integer values to map to operational pathways.
* **Operands**: Accepts whole integer values (`int`).
* **Division Behavior**: Performs integer division (truncates decimals). For example, 5 / 2 will result in 2.

