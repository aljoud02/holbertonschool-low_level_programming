# C - Recursion

A project designed to learn and implement recursion in the C programming language without using loops.

## Requirements
* Compiles on Ubuntu 20.04 LTS using `gcc`.
* Follows the Betty coding style.
* No loops, static variables, or standard library functions allowed.

## Tasks
* `0-puts_recursion.c`: Prints a string followed by a new line.

