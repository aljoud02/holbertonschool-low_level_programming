# C - Structures and typedef

This project is part of the low-level programming curriculum. It covers the basics of using structures (`struct`) and `typedef` in C programming language.

## Tasks Overview
* **0-dog.h**: Define a new type `struct dog`.

